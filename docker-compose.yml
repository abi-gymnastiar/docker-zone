services:
  dashboard:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      LISTEN_ADDR: ":8080"
      DOCKER_SOCKET: "/var/run/docker.sock"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
